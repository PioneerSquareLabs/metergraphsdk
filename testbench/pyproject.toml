[project]
name = "metergraph-live-testbench"
version = "0.1.0"
description = "Live cross-provider cost-tracking test bench for Metergraph"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40",
    "google-genai>=1",
    "openai>=1",
]
